# 팀 작업 규칙

이 문서는 세 명이 같은 코드를 안전하게 수정하기 위한 약속입니다.

## 1. 작업을 시작할 때

1. 칸반보드에서 자신이 맡을 카드를 확인합니다.
2. 카드의 `담당자`에 자신의 이름을 적습니다.
3. 카드 상태를 `Todo`에서 `In Progress`로 옮깁니다.
4. 최신 `main`을 받은 뒤 새 브랜치를 만듭니다.

```bash
git switch main
git pull origin main
git switch -c feature/작업이름
```

브랜치 이름은 다음처럼 사용합니다.

- 기능 추가: `feature/작업이름`
- 오류 수정: `fix/작업이름`
- 문서 작업: `docs/작업이름`

`main`에서는 직접 코드를 수정하거나 push하지 않습니다.

## 2. 작업 중

- 한 브랜치에서는 한 가지 작업만 진행합니다.
- 다른 팀원이 수정 중인 파일을 함께 고쳐야 한다면 먼저 카톡으로 알립니다.
- 변경 내용을 이해하기 쉽도록 작은 단위로 commit합니다.
- commit 내용은 무엇을 바꿨는지 알아볼 수 있게 작성합니다.

예시:

```text
게임 일시정지 기능 추가
스코어보드 정렬 오류 수정
작업 규칙 문서 작성
```

## 3. 작업을 마쳤을 때

1. 직접 실행하여 변경한 기능을 확인합니다.
2. 테스트가 있다면 실행합니다.
3. 자신의 브랜치를 push합니다.
4. `main`으로 보내는 Pull Request를 만듭니다.
5. 칸반 카드를 `Review`로 옮깁니다.
6. 다른 팀원 한 명 이상이 내용을 확인합니다.
7. 문제가 없으면 팀장이 `main`에 합칩니다.
8. 합친 뒤 칸반 카드를 `Done`으로 옮깁니다.

## 4. Pull Request 확인 규칙

확인하는 팀원은 다음 내용을 봅니다.

- 요청한 기능이 실제로 동작하는가?
- 기존 기능이 망가지지 않았는가?
- 불필요하거나 이해하기 어려운 코드가 없는가?
- 확인 방법과 결과가 적혀 있는가?

수정이 필요하면 이유와 수정할 부분을 댓글로 남깁니다.

## 5. 충돌이 생겼을 때

- 어느 한쪽 코드를 임의로 삭제하지 않습니다.
- 충돌한 코드를 작성한 팀원과 먼저 확인합니다.
- 해결한 뒤 두 기능이 모두 동작하는지 다시 실행합니다.

## 6. 기록 남기기

- 작업 카드는 관련 브랜치와 Pull Request에 연결합니다.
- 중요한 결정은 카톡으로 끝내지 않고 카드나 문서에도 짧게 남깁니다.
- 오류가 발생하면 원인과 해결 방법을 기록합니다.

